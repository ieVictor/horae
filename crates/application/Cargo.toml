[package]
name = "horae-application"
version.workspace = true
edition.workspace = true

[dependencies]
horae-domain = { path = "../domain" }
serde = { version = "1", features = ["derive"] }
async-trait = "0.1"
uuid = { version = "1", features = ["v4"] }
